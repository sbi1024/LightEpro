<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.LightEpro.sch.mapper.SchMapper003">
    <select id="selectScheduleCount" parameterType="com.example.LightEpro.sch.dto.sch003.SchRqDto003" resultType="int">
        /* selectScheduleCount */
        SELECT COUNT(1)
        FROM T_SC_SCH TSS
        WHERE 1 = 1
          AND TSS.SCHM_SEQ = #{schedule.schmSeq}
          AND TSS.SCH_SEQ = #{schedule.schSeq}
          AND TSS.USE_YND = 'Y'
    </select>

    <update id="updateSchedule" parameterType="com.example.LightEpro.sch.dto.sch003.SchRqDto003">
        /* updateSchedule */
        UPDATE T_SC_SCH TSS
        SET TSS.USE_YND     = 'N',
            TSS.MODIFY_DATE = NOW(),
            TSS.MODIFY_SEQ  = #{user.userSeq}
        WHERE 1 = 1
          AND TSS.SCHM_SEQ = #{schedule.schmSeq}
          AND TSS.SCH_SEQ = #{schedule.schSeq}
          AND TSS.USE_YND = 'Y'
    </update>

    <update id="updateScheduleUsers" parameterType="com.example.LightEpro.sch.dto.sch003.SchRqDto003">
        /* updateScheduleUsers */
        UPDATE T_SC_SCH_USER TSSU
        SET TSSU.USE_YND     = 'N',
            TSSU.MODIFY_DATE = NOW(),
            TSSU.MODIFY_SEQ  = #{user.userSeq}
        WHERE 1 = 1
          AND TSSU.SCHM_SEQ = #{schedule.schmSeq}
          AND TSSU.SCH_SEQ = #{schedule.schSeq}
          AND TSSU.USE_YND = 'Y'
    </update>
</mapper>