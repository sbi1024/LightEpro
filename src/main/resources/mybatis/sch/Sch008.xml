<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.LightEpro.sch.mapper.SchMapper008">
    <select id="selectUserCount" resultType="int">
        /* selectUserCount */
        SELECT COUNT(1)
        FROM T_EM_COMP TEC
                 INNER JOIN T_EM_DEPT TED
                            ON TEC.COMP_SEQ = TED.COMP_SEQ
                                AND TEC.USE_YND = 'Y'
                                AND TED.USE_YND = 'Y'
                 INNER JOIN T_EM_EMP TEE
                            ON TED.DEPT_SEQ = TEE.DEPT_SEQ
                                AND TEE.USE_YND = 'Y'
                 INNER JOIN T_EM_EMP_MAPPING TEEM
                            ON TEC.COMP_SEQ = TEEM.COMP_SEQ
                                AND TED.DEPT_SEQ = TEEM.DEPT_SEQ
                                AND TEE.EMP_SEQ = TEEM.EMP_SEQ
                                AND TEEM.USE_YND = 'Y'
                 INNER JOIN T_EM_EMP_ACCOUNT TEEA
                            ON TEEM.EMP_SEQ = TEEA.EMP_SEQ
                                AND TEEA.USE_YND = 'Y'
        WHERE 1 = 1
          AND TEC.COMP_SEQ = #{user.userCompSeq}
          AND TED.DEPT_SEQ = #{user.userDeptSeq}
          AND TEE.EMP_SEQ = #{user.userSeq}
    </select>

    <select id="selectCalendarCount" resultType="int">
        /* selectCalendarCount */
        SELECT COUNT(1)
        FROM T_SC_CAL TSC
        WHERE 1 = 1
          AND TSC.CAL_SEQ = #{calendar.calSeq}
          AND TSC.USE_YND = 'Y'
    </select>

    <update id = "updateCalendarAndSchedulesAndUsers">
        /* updateCalendarAndSchedulesAndUsers */
        UPDATE T_SC_CAL TSC
                 INNER JOIN T_SC_CAL_USER TSCU
                            ON TSC.CAL_SEQ = TSCU.CAL_SEQ
                                AND TSC.USE_YND = 'Y'
                                AND TSCU.USE_YND = 'Y'
                 INNER JOIN T_SC_SCH TSS
                            ON TSC.CAL_SEQ = TSS.CAL_SEQ
                                AND TSS.USE_YND = 'Y'
                 INNER JOIN T_SC_SCH_USER TSSU
                            ON TSS.SCHM_SEQ = TSSU.SCHM_SEQ
                                AND TSS.SCH_SEQ = TSSU.SCH_SEQ
                                AND TSSU.USE_YND = 'Y'
        SET TSC.USE_YND     = 'N',
            TSC.MODIFY_DATE = NOW(),
            TSC.MODIFY_SEQ  = #{user.userSeq},
            TSCU.USE_YND     = 'N',
            TSCU.MODIFY_DATE = NOW(),
            TSCU.MODIFY_SEQ  = #{user.userSeq},
            TSS.USE_YND     = 'N',
            TSS.MODIFY_DATE = NOW(),
            TSS.MODIFY_SEQ  = #{user.userSeq},
            TSSU.USE_YND     = 'N',
            TSSU.MODIFY_DATE = NOW(),
            TSSU.MODIFY_SEQ  = #{user.userSeq}
        WHERE 1 = 1
            AND TSC.CAL_SEQ = #{calendar.calSeq}
    </update>
</mapper>